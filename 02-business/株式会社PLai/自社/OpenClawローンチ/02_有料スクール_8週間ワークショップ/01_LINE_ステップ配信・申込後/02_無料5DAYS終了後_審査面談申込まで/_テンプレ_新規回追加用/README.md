# 新規回追加用テンプレ

1. このフォルダ一式を `第N回_（無料側と同じフォルダ名）` にコピーする。
2. `A-1_…` をその回のクロージングに合わせて書き換える。
3. `開催中_Day3`〜`Day5` を、**その回の Day3〜5 で話す有料スクール詳細**に合わせて埋める。
4. セミナー**終了後**の追い配信は `../00_全回共通/A-2〜A-6` を参照（必要なら文言だけ微修正）。
